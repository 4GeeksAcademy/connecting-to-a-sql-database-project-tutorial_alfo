Indicaciones sobre lo que hice:

Para la primera práctica creé el archivo 'postgre.py' dentro del directorio /src.

La última parte de la práctica que consiste en imprimir en la terminal a modo de dataframe una de las tablas sql, 
la realicé en el archivo python llamado 'postgres2pandas.py'.

Los 'con.execute' estan en modo anotación para que no se repitiesen los comandos.

Los ejercicios de sqlite3 y postgres con la base de datos del titanic están realizadas dentro de los archivos 
'titanic_postgre.py' y 'titanic_sqlite3.py'.